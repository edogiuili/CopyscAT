% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth.R
\name{smoothClusters}
\alias{smoothClusters}
\title{Smooth copy number calls within cell clusters}
\usage{
smoothClusters(
  inputClusters,
  inputCNVList,
  inputCNVClusterFile = "",
  percentPositive = 0.5,
  removeEmpty = TRUE
)
}
\arguments{
\item{inputClusters}{Two-column data frame of barcode and cluster label.}

\item{inputCNVList}{Per-cell copy number estimates, normally
\code{final_cnv_list[[3]]} from \code{\link[=annotateCNV4]{annotateCNV4()}}.}

\item{inputCNVClusterFile}{Optional path to a CSV of copy number calls, used
instead of \code{inputCNVList}.}

\item{percentPositive}{Fraction of a cluster that must carry an alteration
for the cluster to be called altered.}

\item{removeEmpty}{Drop arms where every cluster has the same call.}
}
\value{
A data frame of barcodes with their cluster-level copy number calls.
}
\description{
Replaces per-cell copy number estimates with a per-cluster consensus, which
suppresses the noise inherent in single-cell ATAC coverage. A cluster is
called altered on an arm when more than \code{percentPositive} of its cells are.
}
\examples{
\dontrun{
smoothedCNVList <- smoothClusters(
  scDataSampClusters, inputCNVList = final_cnv_list[[3]],
  percentPositive = 0.4
)
}
}
\seealso{
Other CNV annotation: 
\code{\link{annotateCNV3}()},
\code{\link{annotateCNV4}()},
\code{\link{annotateCNV4B}()}
}
\concept{CNV annotation}
