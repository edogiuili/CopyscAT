% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neoplastic.R
\name{identifyNonNeoplastic}
\alias{identifyNonNeoplastic}
\title{Separate neoplastic from non-neoplastic cells}
\usage{
identifyNonNeoplastic(
  inputMatrix,
  estimatedCellularity = 0.8,
  nmfComponents = 5,
  outputHeatmap = TRUE,
  cutHeight = 0.6,
  methodHclust = "ward.D"
)
}
\arguments{
\item{inputMatrix}{Collapsed, filtered matrix from \code{\link[=filterCells]{filterCells()}}.}

\item{estimatedCellularity}{Expected tumour cellularity. Works best below
about 0.9.}

\item{nmfComponents}{Number of NMF components.}

\item{outputHeatmap}{Write a heatmap and violin plot to the output path.}

\item{cutHeight}{Dendrogram cut height, as a fraction of maximum height.}

\item{methodHclust}{Linkage method passed to \code{\link[fastcluster:hclust]{fastcluster::hclust()}}.}
}
\value{
A list with \code{cellAssigns} (cluster per barcode), \code{normalBarcodes}
and \code{clusterNormal} (the index of the non-neoplastic cluster).
}
\description{
Factorises the collapsed arm matrix with \pkg{NMF}, clusters cells on the
factor loadings, and treats the cluster with the lowest variance across arms
as the non-neoplastic population. Use the returned barcodes as the baseline
for \code{\link[=identifyCNVClusters]{identifyCNVClusters()}} or \code{\link[=annotateCNV4B]{annotateCNV4B()}}.
}
\details{
Inspect the heatmap it writes and adjust \code{cutHeight} and \code{nmfComponents}
when the clusters do not separate cleanly.
}
\examples{
\dontrun{
nmf_results <- identifyNonNeoplastic(
  scData_collapse, methodHclust = "ward.D", cutHeight = 0.4
)
}
}
\seealso{
Other neoplastic classification: 
\code{\link{estimateCellCycleFraction}()}
}
\concept{neoplastic classification}
