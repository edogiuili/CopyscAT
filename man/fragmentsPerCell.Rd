% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fragment-qc.R
\name{fragmentsPerCell}
\alias{fragmentsPerCell}
\title{Fragment counts per cell from fragment files}
\usage{
fragmentsPerCell(files, minFragments = 0)
}
\arguments{
\item{files}{Named character vector of paths. Names are used as sample
labels. A path ending in \code{.csv} is read as a CellRanger
\code{singlecell.csv}; anything else is treated as a fragment file.}

\item{minFragments}{Drop barcodes with fewer fragments than this. Default
\code{0}, which keeps everything.}
}
\value{
A data frame with columns \code{sample}, \code{barcode} and \code{n_fragments}.
}
\description{
Counts the fragments recorded for each barcode. Unlike \code{\link[=signalPerCell]{signalPerCell()}},
which reports base pairs covered, this is the number of fragments and so is
directly comparable with the \code{minFrags} argument of
\code{\link[=initialiseEnvironment]{initialiseEnvironment()}} and the \code{-f} option of
\code{process_fragment_file.py}.
}
\details{
Reads either a 10x \code{fragments.tsv.gz} (barcode in the fourth column,
\verb{#} comment lines skipped) or a CellRanger \code{singlecell.csv}. For a
\code{singlecell.csv} the \code{passed_filters} column is used, which counts only
fragments passing CellRanger's own filters and so will not exactly match a
direct count of the fragment file.
}
\examples{
\dontrun{
frags <- fragmentsPerCell(
  c(tumour1 = "tumour1/fragments.tsv.gz"),
  minFragments = 1000
)
plotCellDistribution(frags, "n_fragments", cutoff = 1e4)
}
}
\seealso{
\code{\link[=signalPerCell]{signalPerCell()}} for the base-pair signal already in a CopyscAT
matrix.

Other quality control: 
\code{\link{filterCells}()},
\code{\link{plotCellDistribution}()},
\code{\link{plotCellKnee}()},
\code{\link{signalPerCell}()},
\code{\link{summariseCellDepth}()}
}
\concept{quality control}
