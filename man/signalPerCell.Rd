% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fragment-qc.R
\name{signalPerCell}
\alias{signalPerCell}
\title{Per-cell signal from one or more CopyscAT matrices}
\usage{
signalPerCell(matrices, sep = "\\t")
}
\arguments{
\item{matrices}{Either a named list or character vector of matrix file
paths, or a named list of already-loaded data frames. Names are used as
sample labels.}

\item{sep}{Field separator, when reading from file. Default tab.}
}
\value{
A data frame with one row per cell and columns \code{sample}, \code{barcode},
\code{signal_bp} (total base pairs covered) and \code{nonzero_bins} (bins with any
signal).
}
\description{
Summarises how much signal each barcode carries, so that sequencing depth
can be compared across samples before running the CNV workflow.
}
\details{
The values are \strong{summed fragment lengths in base pairs}, not fragment
counts. \code{process_fragment_file.py} accumulates \code{end - start - 1} for every
fragment, so the row sum of a CopyscAT matrix is the total genomic span
covered by a cell. Use \code{\link[=fragmentsPerCell]{fragmentsPerCell()}} when you need actual fragment
counts.
}
\examples{
data(scDataSamp)
signal <- signalPerCell(list(demo = scDataSamp))
summary(signal$signal_bp)
}
\seealso{
\code{\link[=fragmentsPerCell]{fragmentsPerCell()}} for true fragment counts,
\code{\link[=plotCellDistribution]{plotCellDistribution()}} and \code{\link[=plotCellKnee]{plotCellKnee()}} to visualise the result.

Other quality control: 
\code{\link{filterCells}()},
\code{\link{fragmentsPerCell}()},
\code{\link{plotCellDistribution}()},
\code{\link{plotCellKnee}()},
\code{\link{summariseCellDepth}()}
}
\concept{quality control}
