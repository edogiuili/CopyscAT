% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fragment-qc.R
\name{plotCellDistribution}
\alias{plotCellDistribution}
\title{Plot the per-cell depth distribution}
\usage{
plotCellDistribution(
  cellData,
  valueColumn = NULL,
  cutoff = NULL,
  bins = 60,
  logScale = TRUE
)
}
\arguments{
\item{cellData}{Output of \code{\link[=signalPerCell]{signalPerCell()}} or \code{\link[=fragmentsPerCell]{fragmentsPerCell()}}.}

\item{valueColumn}{Column to plot. Defaults to whichever of \code{signal_bp} or
\code{n_fragments} is present.}

\item{cutoff}{Optional threshold to mark with a dashed line, for example the
\code{minFrags} you intend to use.}

\item{bins}{Number of histogram bins.}

\item{logScale}{Use a log10 x axis. Default \code{TRUE}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Draws one histogram per sample on a shared log scale, which makes the split
between real cells and ambient barcodes easy to see. A bimodal shape is
normal: the left mode is empty or ambient barcodes, the right mode cells.
}
\examples{
data(scDataSamp)
plotCellDistribution(signalPerCell(list(demo = scDataSamp)))
}
\seealso{
Other quality control: 
\code{\link{filterCells}()},
\code{\link{fragmentsPerCell}()},
\code{\link{plotCellKnee}()},
\code{\link{signalPerCell}()},
\code{\link{summariseCellDepth}()}
}
\concept{quality control}
