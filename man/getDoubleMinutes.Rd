% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/double-minutes.R
\name{getDoubleMinutes}
\alias{getDoubleMinutes}
\title{Detect focal amplifications in a single cell}
\usage{
getDoubleMinutes(
  inputMatrix,
  targetCell,
  doPlot = FALSE,
  penalty_type = "SIC",
  doLosses = FALSE,
  peakCutoff = 5,
  lossCutoff = -1,
  minThreshold = 4
)
}
\arguments{
\item{inputMatrix}{Single-chromosome matrix with a \code{loc1} column of bin
labels and one column per cell.}

\item{targetCell}{Column index of the cell to analyse.}

\item{doPlot}{Draw the changepoint fit. Default \code{FALSE}.}

\item{penalty_type}{Penalty passed to \code{\link[changepoint:cpt.meanvar]{changepoint::cpt.meanvar()}}.}

\item{doLosses}{Return losses below \code{lossCutoff} instead of gains.}

\item{peakCutoff}{Minimum segment Z-score to call an amplification.}

\item{lossCutoff}{Maximum segment Z-score to call a loss.}

\item{minThreshold}{Skip the cell unless its maximum Z-score exceeds this.
Raising it speeds up large datasets at the cost of sensitivity.}
}
\value{
A character vector of interval labels (\code{chrom_start.chrom_end}), or
\code{NULL} when the cell has no qualifying segment.
}
\description{
Scales one cell's signal on one chromosome to a robust Z-score, then runs
changepoint analysis (\code{\link[changepoint:cpt.meanvar]{changepoint::cpt.meanvar()}}) to find segments whose
mean exceeds \code{peakCutoff} (amplifications) or falls below \code{lossCutoff}
(losses). Used per cell by \code{\link[=identifyDoubleMinutes]{identifyDoubleMinutes()}}.
}
\seealso{
Other focal amplification: 
\code{\link{identifyDoubleMinutes}()},
\code{\link{testOverlap}()}
}
\concept{focal amplification}
