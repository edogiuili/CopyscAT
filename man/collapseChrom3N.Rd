% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapse.R
\name{collapseChrom3N}
\alias{collapseChrom3N}
\title{Collapse binned signal to chromosome arms}
\usage{
collapseChrom3N(
  inputMatrix,
  minimumSegments = 40,
  summaryFunction = cutAverage,
  logTrans = FALSE,
  binExpand = 1,
  minimumChromValue = 2,
  tssEnrich = 5,
  logBase = 2,
  minCPG = 300,
  powVal = 0.73
)
}
\arguments{
\item{inputMatrix}{Normalised matrix from \code{\link[=normalizeMatrixN]{normalizeMatrixN()}}.}

\item{minimumSegments}{Unused; kept for backward compatibility.}

\item{summaryFunction}{Function used to summarise each arm. Default
\code{\link[=cutAverage]{cutAverage()}}.}

\item{logTrans}{Apply a log transform to the CpG scaling factor.}

\item{binExpand}{Merge this many adjacent bins before collapsing. \code{1}
leaves bins untouched.}

\item{minimumChromValue}{Drop arms whose median signal falls below this.}

\item{tssEnrich}{Weight applied to the CpG scaling factor.}

\item{logBase}{Log base used when \code{logTrans = TRUE}.}

\item{minCPG}{Drop bins with CpG density below this.}

\item{powVal}{Exponent of the CpG correction. \code{0.70}-\code{0.75} suits hg19 and
hg38.}
}
\value{
A data frame with a \code{chrom} column of arm labels, one column per
cell, and a \code{medianNorm} column.
}
\description{
Removes centromeric and blacklisted bins, divides signal by a power
transform of CpG density to correct the accessibility bias, then summarises
each chromosome arm per cell. Also infers sample sex from the X/Y signal
difference and records it in \code{scCNVCaller$isMale}.
}
\examples{
\dontrun{
scData_collapse <- collapseChrom3N(
  scData_k_norm, summaryFunction = cutAverage,
  minimumChromValue = 100, minCPG = 300, powVal = 0.73
)
}
}
\seealso{
Other normalisation: 
\code{\link{computeCenters}()},
\code{\link{normalizeMatrixN}()},
\code{\link{scaleMatrix}()}
}
\concept{normalisation}
