% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/double-minutes.R
\name{identifyDoubleMinutes}
\alias{identifyDoubleMinutes}
\title{Detect recurrent focal amplifications across all cells}
\usage{
identifyDoubleMinutes(
  inputMatrix,
  minCells = 100,
  qualityCutoff2 = 100,
  peakCutoff = 5,
  lossCutoff = -1,
  doPlots = FALSE,
  imageNumber = 1000,
  logTrans = FALSE,
  cpgTransform = FALSE,
  doLosses = FALSE,
  minThreshold = 4
)
}
\arguments{
\item{inputMatrix}{Normalised matrix from \code{\link[=normalizeMatrixN]{normalizeMatrixN()}}.}

\item{minCells}{Minimum cells carrying an event to keep it (first pass).}

\item{qualityCutoff2}{Minimum cells carrying an event to keep it (final
pass).}

\item{peakCutoff}{Minimum segment Z-score to call an amplification.}

\item{lossCutoff}{Maximum segment Z-score to call a loss.}

\item{doPlots}{Write a QC plot every \code{imageNumber} cells.}

\item{imageNumber}{Plot interval, in cells.}

\item{logTrans}{Treat the input as log-transformed when scaling by CpG.}

\item{cpgTransform}{Divide signal by CpG density before calling.}

\item{doLosses}{Call losses instead of gains.}

\item{minThreshold}{Z-score below which a cell is skipped.}
}
\value{
A data frame with one row per cell and one logical column per
retained event, or \code{NULL} when nothing passes the filters.
}
\description{
Runs \code{\link[=getDoubleMinutes]{getDoubleMinutes()}} on every cell and chromosome, merges overlapping
intervals, and keeps only events recurring in enough cells. Slow on large
datasets; raise \code{minThreshold} to speed it up.
}
\examples{
\dontrun{
dm_candidates <- identifyDoubleMinutes(
  scData_k_norm, minCells = 100, qualityCutoff2 = 100, minThreshold = 4
)
}
}
\seealso{
Other focal amplification: 
\code{\link{getDoubleMinutes}()},
\code{\link{testOverlap}()}
}
\concept{focal amplification}
