% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{initialiseEnvironment}
\alias{initialiseEnvironment}
\title{Load genome references and analysis parameters for a session}
\usage{
initialiseEnvironment(
  genomeFile,
  cytobandFile,
  cpgFile,
  binSize = 1e+06,
  minFrags = 10000,
  cellSuffix = c("-1"),
  lowerTrim = 0.5,
  upperTrim = 0.8
)
}
\arguments{
\item{genomeFile}{Path to a two-column chromosome-size table
(chromosome, length), tab separated and without a header.}

\item{cytobandFile}{Path to the binned cytoband table produced by
\code{\link[=generateReferences]{generateReferences()}}.}

\item{cpgFile}{Path to the binned CpG density table produced by
\code{\link[=generateReferences]{generateReferences()}}. Four columns: chromosome, start, end, density.}

\item{binSize}{Genomic bin size in base pairs. Must match the \code{tileWidth}
used to build the reference files. Default \code{1e6}.}

\item{minFrags}{Minimum number of fragments for a cell to be considered.}

\item{cellSuffix}{Character vector of barcode suffixes present in the input
matrix, for example \code{c("-1", "-2")}.}

\item{lowerTrim}{Lower quantile for the trimmed mean used by \code{\link[=cutAverage]{cutAverage()}}.}

\item{upperTrim}{Upper quantile for the trimmed mean used by \code{\link[=cutAverage]{cutAverage()}}.}
}
\value{
Invisibly \code{TRUE}. Called for the side effect of populating
\link{scCNVCaller}.
}
\description{
Reads the three reference tables produced by \code{\link[=generateReferences]{generateReferences()}} and
stores them, together with the binning parameters, in the \link{scCNVCaller}
session environment. Run this once per R session before any other step.
}
\examples{
\dontrun{
initialiseEnvironment(
  genomeFile   = "hg38_chrom_sizes.tsv",
  cytobandFile = "hg38_1e+06_cytoband_densities_granges.tsv",
  cpgFile      = "hg38_1e+06_cpg_densities.tsv",
  binSize      = 1e6,
  cellSuffix   = c("-1")
)
}
}
\seealso{
Other session setup: 
\code{\link{generateReferences}()},
\code{\link{initializeStandards}()},
\code{\link{setOutputFile}()}
}
\concept{session setup}
