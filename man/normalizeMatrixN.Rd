% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{normalizeMatrixN}
\alias{normalizeMatrixN}
\title{Normalise a raw single-cell fragment matrix}
\usage{
normalizeMatrixN(
  inputMatrix,
  logNorm = FALSE,
  maxZero = 2000,
  imputeZeros = FALSE,
  blacklistProp = 0.8,
  priorCount = 1,
  blacklistCutoff = 100,
  dividingFactor = 1e+06,
  upperFilterQuantile = 0.95
)
}
\arguments{
\item{inputMatrix}{Raw count matrix from \code{\link[=readInputTable]{readInputTable()}}, cells in rows
and bins in columns.}

\item{logNorm}{Log-transform during CPM normalisation. Default \code{FALSE}.}

\item{maxZero}{Maximum number of empty bins tolerated per cell, after
uninformative bins are excluded.}

\item{imputeZeros}{Replace zero counts with the per-bin median. Off by
default and not recommended.}

\item{blacklistProp}{Proportion of cells that must fall below
\code{blacklistCutoff} for a bin to be treated as uninformative. Lower is
stricter.}

\item{priorCount}{Prior count added before log normalisation. Only used
when \code{logNorm = TRUE}.}

\item{blacklistCutoff}{Signal below which a bin counts as empty in a cell.}

\item{dividingFactor}{Deprecated. Scaling divisor applied after CPM; set to
\code{1} to disable.}

\item{upperFilterQuantile}{Cells with total signal above this quantile are
dropped as likely doublets.}
}
\value{
A data frame with \code{chrom} and \code{pos} columns, a logical \code{blacklist}
column, a \code{raw_medians} column, and one column per surviving cell.
}
\description{
First analysis step. Drops outlier cells above \code{upperFilterQuantile},
flags uninformative ("blacklist") bins that are near-empty across most
cells, removes cells with too many empty bins, then scales to counts per
million with \code{\link[edgeR:cpm]{edgeR::cpm()}}.
}
\examples{
\dontrun{
scData_k_norm <- normalizeMatrixN(
  scDataSamp,
  blacklistProp = 0.8, blacklistCutoff = 125, dividingFactor = 1
)
}
}
\seealso{
Other normalisation: 
\code{\link{collapseChrom3N}()},
\code{\link{computeCenters}()},
\code{\link{scaleMatrix}()}
}
\concept{normalisation}
